const { Product } = require("../../../models/products.models");
const Razorpay = require("razorpay");
const shortid = require("shortid");
const { calculateTotalAmt } = require("../../../helper/calculateTotalAmt");

// MAKE PAYMENT WITH RAZORPAY
const makePaymentWithRazorpay = async (req, res) => {
 try {
  
 } catch (err) {
  console.log(error);
 }
};



module.exports = {
  makePaymentWithRazorpay
};
